<!DOCTYPE html>
<html>
  <head>
    <title>New Polling Unit Result</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-light">
    <div class="container mt-5">
      <h1 class="mb-4 text-center">Add New Polling Unit Result</h1>

      <form method="post" action="/polling-unit/save">
        <div class="mb-3">
          <label class="form-label">Polling Unit ID</label>
          <input
            type="text"
            name="polling_unit_uniqueid"
            class="form-control"
            required
          />
        </div>
        <div class="mb-3">
          <label class="form-label">Party Abbreviation</label>
          <input
            type="text"
            name="party_abbreviation"
            class="form-control"
            required
          />
        </div>
        <div class="mb-3">
          <label class="form-label">Party Score</label>
          <input
            type="number"
            name="party_score"
            class="form-control"
            required
          />
        </div>
        <button type="submit" class="btn btn-success w-100">Save Result</button>
      </form>

      <a href="/lga-results" class="btn btn-secondary w-100 mt-3"
        >Back to LGA Results</a
      >
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
<!-- This is a simple form to add a new polling unit result. It includes fields for the polling unit ID, party abbreviation, and party score. The form submits to the /polling-unit/save endpoint. -->
